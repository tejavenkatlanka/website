---
interface Props {
  tags: string[]
}
const { tags } = Astro.props
---

{
  tags.map((tag: string) => {
    return (
      <span class="text-xs text-black inline-flex items-center rounded-md p-1 bg-gray-500/10">
        {tag}
      </span>
    )
  })
}
